# Here are your Instructions


## o start frontend 
`cd frontend`
update backend url in .env when need
`npm start`
http://localhost:3000/


## to start backend and db
`docker compose up -d --build`
backend running on http://localhost:8002
for swagger docs http://localhost:8002/docs